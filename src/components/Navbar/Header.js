import Navbar from "./Navbar";

const Header = () => {
    return(
        <header>
            <div className="navigation-area">
                <Navbar />
            </div>
        </header>
    );
};

export default Header;