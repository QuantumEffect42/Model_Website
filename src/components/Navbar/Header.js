import Navbar from "./Navbar";
import "./Navbar.css"

const Header = () => {
    return(
        <header>
            <div className="navigation-area">
                <Navbar />
            </div>
        </header>
    );
};

export default Header;